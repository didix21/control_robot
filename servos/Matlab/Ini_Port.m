function[s]= Ini_Port ()

% initialize Port Communication
%Connect via USB
%s = serial('COM4', 'BaudRate', 9600);

%Connect via Bluetooth
%s = Bluetooth('XURRA_BT',1);
s='Falta conectar'
pause (1);
end